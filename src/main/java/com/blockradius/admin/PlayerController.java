package com.blockradius.admin;

/**
 * Legacy stub.
 *
 * NOTE:
 * The real player routes now live in:
 *   com.blockradius.admin.controller.PlayerController
 *
 * This class is kept only so old references compile,
 * but it is NOT a Spring @Controller anymore.
 */
public class PlayerController {
    // intentionally empty
}
